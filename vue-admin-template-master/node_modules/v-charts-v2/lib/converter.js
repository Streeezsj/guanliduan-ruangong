(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (factory((global.Converter = {})));
}(this, (function (exports) { 'use strict';

  var simpleDataConverter = function simpleDataConverter(data) {
    if (!data || !data.columns || !data.rows) return data;
    var columns = data.columns,
        rows = data.rows;

    var result = { columns: columns };
    result.rows = rows.map(function (row) {
      var temp = {};
      row.forEach(function (val, index) {
        temp[columns[index]] = val;
      });
      return temp;
    });
    return result;
  };

  exports.simpleDataConverter = simpleDataConverter;

  Object.defineProperty(exports, '__esModule', { value: true });

})));
